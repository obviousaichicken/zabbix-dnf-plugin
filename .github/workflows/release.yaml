name: Build and Publish Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  checks:
    name: Pre-release Checks
    uses: ./.github/workflows/checks.yaml

  integration:
    name: DNF Integration Tests
    uses: ./.github/workflows/dnf-integration.yaml

  release:
    name: Build and Publish Release
    needs:
      - checks
      - integration
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false

      - name: Build plugin
        env:
          CGO_ENABLED: '0'
          GOARCH: amd64
          GOOS: linux
        run: |
          mkdir -p dist
          go build -trimpath -ldflags='-s -w' \
            -o dist/zabbix-dnf-plugin ./cmd/agent

      - name: Create checksum
        run: |
          cd dist
          sha256sum zabbix-dnf-plugin > zabbix-dnf-plugin.sha256

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            dist/zabbix-dnf-plugin \
            dist/zabbix-dnf-plugin.sha256 \
            dnf.conf \
            install.sh \
            template-dnf-by-zabbix-agent2.yaml \
            --generate-notes \
            --title "$GITHUB_REF_NAME" \
            --verify-tag

  smoke:
    name: Published Release Installation Tests
    needs: release
    uses: ./.github/workflows/release-smoke.yaml
